# Mendaftarkan fungsi utama dari setiap file tab agar bisa dipanggil oleh app.py
from .yield_perf import render_yield_perf
from .yield_sensus import render_yield_sensus
from .yield_periodik import render_yield_periodik